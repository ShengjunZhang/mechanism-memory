[project]
name = "causal-sandbox"
version = "0.1.0"
description = "Small environments for testing causal-learning loops."
requires-python = ">=3.11"
dependencies = []

[project.scripts]
causal-sandbox = "causal_sandbox.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
